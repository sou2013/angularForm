https://www.tektutorialshub.com/angular/how-to-add-bootstrap-to-angular/

install firefox debug extension
do run/add config/ff launch, set url =localhost:4200
in vs, terminal view, start ng serve
set breakpoint in a component.ts 
on left side bar menu, do run, launch localhost, this should kick off page in a new browser page
click submit in the form, should hit breakpoint in onSubmit()
